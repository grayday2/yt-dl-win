# В ytm-dl-src/ уже есть свой .gitignore (от автора проекта): он закрывает
# медиа-артефакты (*.m4a/*.webm/*.jpg), dist/, music/, логи ytm-dl-*.txt,
# собранные ytm-dl-win.zip/.tar.gz и win/wheels/*.whl — в git из wheels
# намеренно лежат только manifest.json и SHA256SUMS.txt.
# Здесь — только то, что живёт ВНЕ ytm-dl-src/ и до 0.6.1 не было покрыто.

# рабочая папка агента: извлечённое дерево патча и диффы (не часть поставки)
.arena-fix1/

# общий мусор
__pycache__/
*.pyc
.DS_Store
*.swp

# состояние очереди лимитов и логи компаньона, если программа запускалась
# прямо из корня репозитория (в ytm-dl-src/ их закрывает свой .gitignore)
.ytm-throttle-queue.json
ytm-dl-log.txt
ytm-dl-error.txt
ytm-dl-diagnostic.txt
ytm-dl-report.txt
ytm-dl-server.log

# артефакты сборки, если собирать из корня, а не из ytm-dl-src/
dist/
ytm-dl-win.zip
ytm-dl-win.tar.gz

# Собранные win-архивы поставки: с 0.6.1 в git лежит только ytm-dl-src-<версия>.zip.
# win-архив воспроизводится из исходников (tools/fetch_wheels.py + tools/build_win_package.sh),
# а каждый положенный в git бинарь остаётся в истории навсегда — README §5.5 фиксирует,
# что однажды это раздуло .git до 119 МБ и снапшот workspace перестал влезать в лимит.
ytm-dl-win-*.zip

# Исключение к правилу выше: ytm-dl-win-0.6.1.zip в git всё-таки лежит (git add -f),
# потому что владельцу нужен файл, который можно скачать с GitHub браузером без git.
# В чистое репо он не переезжает — там этот вопрос решается до начала работы.
